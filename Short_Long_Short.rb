# 2 strings as arguments
# find the longest of the two strings
# concatenate short long short together
# and return the string

def short_long_short(str1, str2)
  str1.length > str.length ? str2 + str1 + str2 : str1 + str2 + str1
end